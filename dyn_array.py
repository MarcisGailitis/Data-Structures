class Array():
    def __init__(self):
        self.arr = ()
        # length user thinks array is
        self.length = 0
        # length of actual array
        self.capacity = 0

    def array(self):
        for i in range(16):
            arr.add[None]

    def array(self, capacity):
        if capacity < 0:
            return IndexError
        self.capacity = capacity

    def size(self):
        return length

    def isEmpty(self):
        return True if length == 0 else False

    def get(self, index):
        return arr[index]

    def set(self, index, elem):
        arr[index] = elem

    def clear(self):
        for item in arr:
            arr[item] = None,
        len = 0


x = Array()

# print(dir(x))

print(x.isEmpty())
